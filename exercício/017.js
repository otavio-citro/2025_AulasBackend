const adicaoarrow = (num1, num2) => { 
    return num1 + num2;
}

const subtracaoarrow = (num1, num2) => { 
    return num1 - num2;
}

const divisaoarrow = (num1, num2) => { 
    return num1 / num2;
}

const multiplicacaoarrow = (num1, num2) => {
    return num1 * num2;
}

console.log(adicaoarrow(10, 10));
console.log(subtracaoarrow(10, 10));
console.log(divisaoarrow(10, 10));
console.log(multiplicacaoarrow(10, 10));
