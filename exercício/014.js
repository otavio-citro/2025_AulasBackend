function areaTriangulo(altura, largura){
    let area = (altura * largura) / 2;
    return area;

}

const areaTrianguloAnonima = function (altura, largura){
    let area = (altura * largura) / 2;
    return area;

}
const areaTrianguloArrow =  (altura, largura) => {
    let area = (altura * largura) / 2;
    return area;
}

